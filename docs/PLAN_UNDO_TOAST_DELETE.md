# Plan: Undo in Toast After Deleting Task & List

Generated by scheduled task on 2026-06-10; re-verified 2026-07-08, 2026-07-29, and 2026-08-05. Covers webapp (desktop + mobile web), native Android, native iOS, scheduled tasks, floater tasks, lists, and floater lists.

## TL;DR

Undo-in-toast is **implemented end-to-end on all three platforms** for tasks (scheduled + floater + recurring instances), lists, and floater lists. The 2026-06-10 gap (web sidebar list delete) was fixed in `588151db` via `use-undoable-list-delete.ts`.

**2026-07-29: one new gap found.** Morning Sweep's "Let it go" action (added `29cd8568`, 2026-07-13) deletes the task **immediately with no toast and no Undo on Android and iOS**; web routes it through the staged path correctly. Fix is a straight port of the existing pattern — see Work item 4. Also one judgement call: web task-step delete (V20 task steps) is immediate with no toast — see Work item 5.

**2026-08-05 status: Work item 4 remains the only open code gap.** New this pass: a small localization/test/refactor punch list (Work item 6). Web Local Mode (added `faa98e29`) verified — undo works there with zero extra code.

## Re-verification notes (2026-08-05)

15 commits since 7/29 audited (security remediation, ScheduledTaskHome/FloaterTaskHome rename, web Local Mode workspace + onboarding Mode step, instance wire-format fixes `a62fe871`/`b7bc5107`, cold-cache optimistic-update fix `2fa7ed33`, sheet/guide polish). Full re-audit of all three platforms:

- **Coverage matrix unchanged. Work item 4 (Morning Sweep "Let it go" on Android + iOS) is still the only open code gap.** Android: `MorningSweepViewModel.letGo()` L75-80 still calls `todoRepository.deleteTodo(todo)` directly (coordinator already injected at L40). iOS: `MorningSweepScreen.swift:113-116` still calls `container.todoRepository.deleteTodo(card)` directly. Web sweep sheet remains staged.
- **Instance wire-format fixes verified compatible**: `delete-calendar-instance-todo.ts` now sends `todoId` + ISO-8601 `instanceDate` (commented in-file); staged flow unchanged.
- **Web Local Mode (`faa98e29`) gets undo for free**: `localApi.ts` intercepts the same `DELETE /api/…` calls (`handleLocalRequest`, dispatch at localTodos/localFloaters/localLists); since undo means the request is never sent, the workspace row survives and the undo refetch restores it. No Local-Mode-specific undo code needed — confirmed by design, not just luck.
- **Core infra untouched**: web `use-undoable-delete.ts` (5s), `use-undoable-list-delete.ts`; Android `UndoableDeleteCoordinator.kt` (8.5s commit / 8s toast, CAS guard, `NonCancellable`); iOS `UndoableDeleteScheduler` (8.5s / 8s). Web unit tests present; **Android/iOS still have zero unit tests for stage/undo/coordinator** (Work item 6).
- **New minor findings this pass** (folded into work items below):
  - Android `task_completed_toast` exists only in base `values/strings.xml` — missing from all 9 translated locale files (`action_undo`, `task_deleted_toast`, `list_deleted_toast` are translated). → Work item 6.
  - iOS commit-failure fallbacks `"Could not delete task."` / `"Could not delete list."` are absent from `Localizable.xcstrings` (English-only). → Work item 6.
  - Web `FloaterListFormSheet.tsx` inlines its own staging against `["floaterListMeta"]` instead of reusing `useUndoableListDelete` — behaviorally correct, refactor candidate. → Work item 6.
  - Web list-delete navigates away at stage time (`/app/tday` or `/app/floater`); Undo restores data but not the route. Same stage-time-navigation choice as mobile (intentional there); on web it means Undo lands you on the fallback page, not back in the list. Cosmetic; note only.
  - Web floater delete toast has no tap-through navigation (unlike scheduled tasks) — intentional, floaters have no date to navigate to.

## Re-verification notes (2026-07-29)

~60 commits since 7/8 audited (R4–R6 feature waves: task steps, Brain Dump, floater-list Reset, resting floaters, Week in Review, Apple Watch, widget overhaul, keyboard/⌘K palette, share extension, QS tile, export/import, Morning Sweep).

- **Core infra unchanged**: web `use-undoable-delete.ts` (5s, settle guard) and `use-undoable-list-delete.ts` untouched; Android `UndoableDeleteCoordinator.kt` still 8.5s commit / `AtomicBoolean` CAS / `NonCancellable` commit (only addition: `showUndoableComplete`, same private path); iOS `SnackbarManager.swift` `UndoableDeleteScheduler` untouched (8.5s vs 8s toast). Unit tests still present.
- **Android unified frosted-pill toast (`1a10a956`)**: action plumbing intact — `SnackbarEvent.actionLabel/onAction` still rendered by `TdayToastHost`, 8s auto-dismiss when an action is present. The new `showInfo()` (no action) is only used for informational toasts.
- **New features audited, no delete surfaces**: Brain Dump (create-only; deselect is local state), keyboard layer/⌘K palette (no delete commands), Apple Watch (read + complete only), widgets (check-off + create deep link only), resting floaters / Week in Review (display-only), share extension / QS tile (create-only), import (merge-only, never deletes).
- **Floater-list Reset (`bd69f895`) is non-destructive**: un-completes floaters and clears completed-history rows; floater-list *delete* still routes through the staged path on all platforms.
- **Task steps (V20)**: whole-task delete with steps is safe — steps are removed only by `ON DELETE CASCADE` at commit time, so undo (refetch) restores them; no need to include steps in the staged record. Individual step delete is immediate/no-toast (Work item 5). Mobile has no steps UI yet.
- **Gap found — Morning Sweep "Let it go"** (`29cd8568`): Android `MorningSweepViewModel.letGo()` and iOS `MorningSweepScreen.swift` call `todoRepository.deleteTodo` directly — no stage, no toast, no Undo. Web's `MorningSweepSheet.tsx` correctly uses the staged `useDeleteTodo`. Work item 4.
- Full call-site audit: all other web `api.DELETE` and Android/iOS repository `delete*` calls for todos/lists/floaters remain the commit half of a staged flow.

## Re-verification notes (2026-07-08)

- **Work item 1 is done.** `ListSidebarSection.tsx` now stages via `useUndoableListDelete` (cache prune + undo toast; DELETE fires only after the toast closes without undo). Hook is shared with the list form sheet.
- **New features since 6/10 checked for regressions:**
  - *List sharing/collaboration* (`fbd7fbf8` etc.): member remove/leave flows are membership changes, not list deletes — correctly outside undo scope; errors toast without Undo.
  - *Toast swipe-to-dismiss* (`666a9864`, iOS `AppRootView`): dismissing only hides the snackbar; the `UndoableDeleteScheduler` commit still fires after its delay. Correct — dismiss ≠ undo.
  - *Swipe gestures* (`c7ee1d14`, iOS `SwipeActions.swift`): swipe-to-delete on scheduled task home/Calendar/TodoList routes through the same `viewModel.delete` → `stageDeleteTodo` undo path. Covered.
- **Completed-history deletes are intentionally immediate** (documented in Android `CompletedViewModel`: "Completed-history deletes stay immediate (no staged undo window)"). iOS matches (immediate `deleteCompletedTodo`); web has no completed-delete action. Noted under work item 3 as an optional consistency decision — iOS shows no success toast where Android does.

## How undo works today (shared pattern)

All platforms use the same client-side **delayed-commit** pattern — there is no backend soft-delete or restore endpoint, and none is needed:

1. **Stage**: item is removed from local cache/query cache only (UI updates instantly). No DELETE request sent.
2. **Toast with Undo**: shown for ~5s (web) / 8s (Android, iOS).
3. **Undo tapped** → staged state restored (refetch on web; staged-record restore on mobile). Server never saw the delete.
4. **Toast expires/dismissed** → real DELETE fires (web) or DELETE pending-mutation is queued and synced (Android/iOS). Race-guarded so commit/undo runs exactly once.

## Current coverage matrix

| Delete flow | Web (desktop + mobile web) | Android | iOS |
|---|---|---|---|
| Scheduled task (today/scheduled task home) | ✅ `features/todayTodos/query/delete-todo.ts` | ✅ `ScheduledTaskHomeViewModel`, `TodoListViewModel` | ✅ `ScheduledTaskHomeViewModel`, `TodoListViewModel` |
| Scheduled task (calendar) | ✅ `features/calendar/query/delete-calendar-todo.ts` | ✅ `CalendarViewModel.delete()` | ✅ `CalendarViewModel.delete()` |
| Recurring instance | ✅ `delete-calendar-instance-todo.ts` | ✅ via `stageDeleteTodo` (instance-aware) | ✅ via `stageDeleteTodo` (instance-aware, matches `instanceDateEpochMs`) |
| Task in list view | ✅ `features/list/query/delete-list-todo.ts` | ✅ `TodoListViewModel.delete()` | ✅ `TodoListViewModel.delete()` |
| Floater task | ✅ `features/floater/query/delete-floater.ts` | ✅ `stageDeleteFloater` path | ✅ `stageDeleteFloater` path |
| List (form/sheet) | ✅ `ListFormSheet.tsx` | ✅ `TodoListViewModel.deleteList()` | ✅ `TodoListViewModel.deleteList()` |
| List (sidebar, single) | ✅ fixed `588151db` — `use-undoable-list-delete.ts` | n/a | n/a |
| Lists (sidebar, bulk) | ✅ fixed `588151db` — same hook | n/a | n/a |
| Task swipe-to-delete | n/a | ✅ same staged path | ✅ `SwipeActions.swift` → staged path |
| Completed-history task | no delete action | ⚠️ immediate by design | ⚠️ immediate by design |
| Floater list | ✅ `FloaterListFormSheet.tsx` | ✅ `FloaterListRepository` | ✅ `FloaterListRepository` |
| Morning Sweep "Let it go" | ✅ staged `useDeleteTodo` | ❌ **Work item 4** | ❌ **Work item 4** |
| Task step (in-editor) | ⚠️ immediate, no toast — Work item 5 | no steps UI | no steps UI |
| Widgets / Watch / palette / share ext. | no delete actions | no delete actions | no delete actions |

Key infrastructure per platform:

- **Web**: `src/hooks/use-undoable-delete.ts` + `use-toast.ts` (Sonner) + `use-todo-action-toast.ts`. i18n keys `undo`, `taskDeleted`, `listDeleted` exist in all 10 locales.
- **Android**: `core/ui/UndoableDeleteCoordinator.kt` (8.5s commit delay, `AtomicBoolean` race guard, own `MainScope` so it survives navigation) + `SnackbarManager`/`TdayToastHost`. Repos expose `stageDelete*` / `undoStaged*` / `delete*`; commit queues `DELETE_*` pending mutations for offline sync.
- **iOS**: `Core/UI/SnackbarManager.swift` `UndoableDeleteScheduler` (8.5s) + `AppSnackbar` in `AppRootView`. Same stage/undo/commit repo methods; SwiftData cache + `PendingMutationEntity` queue.
- **Desktop**: no Electron/Tauri wrapper exists — desktop is the responsive webapp, so web changes cover it. Mobile web likewise.
- **iOS widget**: no delete actions in `TdayWidget` — out of scope.

## Work item 1: undo for sidebar list deletes on web — ✅ DONE (`588151db`, 2026-06-10)

Kept below for reference; implemented essentially as planned (shared `use-undoable-list-delete.ts` hook, stage-time navigation/dialog close, single/bulk, unit tests added).

**File**: `tday-web/src/components/Sidebar/List/ListSidebarSection.tsx` (used by `SidebarContainer.tsx` and `MoreNavigationSheet.tsx`, so it's both the desktop sidebar and the mobile "More" sheet).

Currently `deleteListMutation` and `bulkDeleteListsMutation` call `deleteLists()` (DELETE `/api/list`) immediately in `mutationFn`, then toast without an action. Convert both to the staged pattern already used in `ListFormSheet.tsx`:

1. Extract/reuse the staged-list-delete logic from `ListFormSheet.tsx` (`handleDeleteList`) into a shared hook, e.g. `src/hooks/use-undoable-list-delete.ts`, built on `useUndoableDelete`. It should:
   - Prune `["listMetaData"]` (and `["list", id]`) caches for the requested IDs.
   - Navigate to `/app/tday` if the active list is among them (current `handleDeletedListsSuccess` behavior) — navigate at stage time, like `ListFormSheet` does.
   - `commit`: call `deleteLists(ids)` (keeps existing single/bulk request shape and `deletedIds` response handling).
   - `undo`: `invalidateQueries(["listMetaData"])` (+ `["list"]`, `["todo"]`) to refetch — server never saw the delete, so refetch restores everything including cascaded tasks.
2. Use it in both mutations. Toast copy: `appDict("listDeleted")` for single; for bulk add a pluralized key, e.g. `"listsDeleted": "{{count}} lists deleted"`, to all 10 locale files in `tday-web/messages/`. Action label: existing `t("undo")`.
3. Close the dialogs and clear selection at stage time (current `onSuccess` behavior moves to stage).
4. Edge cases:
   - Don't show "Undo" on error toasts (commit failure should surface the existing destructive toast and invalidate queries).
   - Multiple rapid deletes: `useUndoableDelete` instances are per-toast; verify two overlapping list deletes each commit/undo independently (Sonner supports stacked toasts).
   - Bulk delete navigating away from an active list that's in the selection.

Estimated scope: 1 new hook, edits to 1 component, 10 locale files, plus tests.

## Work item 2: verification checklist (all platforms)

Manual/automated checks to confirm the existing implementations actually behave correctly:

- **Web** (`tday-web/tests/`): unit tests for `use-undoable-delete` double-commit guard if not present; Playwright/RTL checks that DELETE is not sent when Undo is clicked (assert no network call), and is sent exactly once after toast auto-close. Cover: task, calendar task, recurring instance, floater, list (form sheet + sidebar single + sidebar bulk), floater list.
- **Android**: verify recurring-instance delete from calendar restores the correct instance on undo (`instanceDateEpochMs` match); verify undo after navigating away from a deleted list (coordinator's MainScope claim); verify offline: delete → undo with airplane mode on (no pending mutation should remain); delete → commit offline → mutation queued and synced later.
- **iOS**: same matrix as Android; additionally verify the 8.5s commit vs 8s toast timing means the Undo button is always actionable while visible, and that killing the app mid-window leaves the item intact after relaunch (commit never ran — confirm this self-healing is the intended behavior on iOS as it is on Android).
- **Cross-platform copy consistency**: "Task deleted" / "List deleted" / "Floater deleted" — Android uses "Floater deleted" while web uses `taskDeleted` for floaters; decide on one term.

## Work item 3 (optional, consistency/polish)

- **Align undo window**: web ~5s vs Android/iOS 8.5s. Consider 8s web toast duration for delete toasts to match mobile.
- **Completed-history delete consistency**: Android/iOS delete completed items immediately (intentional — no staged undo). iOS shows no success toast where Android does; either add the toast on iOS or document the difference. Web has no completed-delete action at all — decide whether to add one (with the same immediate-delete semantics) or leave as is.
- **Bulk list delete on mobile apps**: Android/iOS have no sidebar bulk delete; if added later, reuse the stage/commit repo methods which already accept lists.
- **Backend (no change recommended)**: endpoints are hard-delete (`DELETE /api/todo`, `/api/todo/instance`, `/api/list`, `/api/floater`, `/api/floaterList`). The client-side delayed-commit makes a server-side restore endpoint unnecessary. Only revisit if a longer undo window (e.g., minutes) or cross-device undo is ever wanted — that would require soft-delete (`deletedAt`) + restore endpoints and is explicitly out of scope here.

## Work item 4: undo for Morning Sweep "Let it go" on Android & iOS — ❌ TODO (found 2026-07-29)

"Let it go" hard-deletes with no toast/Undo on both native apps; web already does it right. Straight port of the existing staged pattern:

**Android** — `android-compose/.../feature/sweep/MorningSweepViewModel.kt` (`letGo()`, ~line 75):

1. `MorningSweepViewModel` already injects `UndoableDeleteCoordinator` (used by `sweepAllToToday`). Replace the direct `todoRepository.deleteTodo(todo)` with the same shape used in `ScheduledTaskHomeViewModel.delete()`:
   - `advancePast(todo)` stays first (card advances immediately).
   - `todoRepository.stageDeleteTodo(todo)` → `coordinator.showUndoableDelete(message = "Task deleted", onUndo = { todoRepository.undoStagedTodoDeletion(todo) }, onCommit = { todoRepository.deleteTodo(todo) })`.
   - On undo, the card should *not* re-enter the sweep deck (deck order is local state; restoring the todo to Today is enough — note this choice in the PR).
2. Recurring instances: `stageDeleteTodo` is instance-aware; pass the card's instance date like other call sites.

**iOS** — `ios-swiftUI/Tday/Feature/Sweep/MorningSweepScreen.swift` (~line 113):

1. Same substitution using `container.undoableDeleteScheduler` + `SnackbarManager`, mirroring `ScheduledTaskHomeViewModel.delete()`: `advance(past: card)` → `container.todoRepository.stageDeleteTodo(card)` → `scheduler.schedule(message: L("Task deleted"), restore: { …undoStagedTodo(staged) }, commit: { try? await …deleteTodo(card) })`. (Note: the iOS restore method is **`undoStagedTodo(_:)`** — `undoStagedTodoDeletion` is the Android name.)
2. Same undo semantics as Android (card doesn't return to the deck).

Tests: Android — coordinator-based unit test asserting no repository `deleteTodo` call when undo fires within the window, exactly one when it expires. iOS — mirror in `TdayTests` if sweep VM tests exist; otherwise manual checklist entry.

Estimated scope: 2 files + tests. No backend, web, shared, or i18n changes (both platforms already have "Task deleted"/Undo strings).

## Work item 5 (decision needed): task-step delete on web is immediate

`TaskStepsSection.tsx` → `useDeleteTaskStep` fires `POST /api/todo/steps/delete` with no toast. Sub-entity, cheap to retype, editor context — reasonable to keep immediate, but it's the only destructive affordance with no feedback at all. Either: (a) wire through `useUndoableDelete` (prune `["taskSteps", todoId]`, commit calls the endpoint, undo invalidates), or (b) document as intentionally immediate alongside the completed-history note. Recommendation: (b) plus at minimum keep it silent-but-instant; revisit if steps gain mobile UI.

## Work item 6: polish punch list (found 2026-08-05)

Small, independent items; none block the feature:

1. **Android translations**: add `task_completed_toast` to the 9 translated `values-*/strings.xml` files (currently base-English only, falls back silently).
2. **iOS localization**: add `"Could not delete task."` and `"Could not delete list."` to `Localizable.xcstrings` (used by every commit-failure closure; currently English-only fallbacks).
3. **Mobile unit tests**: neither platform tests the undo path. Highest value: (a) commit not called when undo fires within window, (b) exactly-once commit on expiry, (c) recurring-instance restore matches `instanceDateEpochMs`, (d) offline delete → undo leaves no pending mutation. Android: coordinator + `stageDelete*` tests beside `TodoRepositoryDeleteCacheTest.kt`. iOS: mirror in `TdayTests`.
4. **Web refactor (optional)**: `FloaterListFormSheet.tsx` should reuse `useUndoableListDelete` instead of its inline staging — same behavior, less duplication.
5. **Copy consistency (carried from Work item 2)**: web uses `taskDeleted` for floaters, Android says "Floater deleted" — pick one term.

## Decisions made autonomously (user not present)

- Treated "desktop" as the responsive webapp (no Electron/Tauri wrapper exists in the repo).
- Scoped backend changes out: client-side delayed-commit is already the established pattern on all platforms; adding server soft-delete would be inconsistent rework for no current benefit.
- Identified the sidebar list delete as the only code change strictly required to satisfy "undo after deleting task and list" everywhere (fixed 6/10); as of 7/29 the only required change is Morning Sweep "Let it go" on Android/iOS (Work item 4).
- Chose "undo restores the task but not its position in the sweep deck" for Work item 4 — deck order is ephemeral local state and re-inserting mid-sweep would be surprising.
- Classified task-step delete as out of the strict task/list scope; left as a documented decision (Work item 5) rather than mandatory work.
