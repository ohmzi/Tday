package com.ohmz.tday.security

import com.ohmz.tday.config.AppConfig

fun testAppConfig(
    authSecret: String = "test-secret-that-is-at-least-32-chars-long!!",
    isProduction: Boolean = false,
    pbkdf2Iterations: Int = 120_000,
    sessionMaxAgeSec: Int = 2_592_000,
    sessionAbsoluteMaxAgeSec: Int = 7_776_000,
    sessionRenewThresholdSec: Int = 604_800,
    passwordProofChallengeTtlSec: Int = 120,
    passwordProofMaxActive: Int = 5000,
    dataEncryptionKeyId: String = "primary",
    dataEncryptionKey: String? = null,
    dataEncryptionKeys: String? = null,
    dataEncryptionAad: String? = null,
    requireEncryptionAtRest: Boolean = false,
    androidPackageName: String = "com.ohmz.tday.compose",
    androidSha256CertFingerprints: List<String> = listOf("AA:BB:CC"),
    probeAppVersion: String? = null,
    probeUpdateRequired: Boolean = false,
    probeCompatibilityMode: String = "exact",
    probeEncryptionKey: String? = null,
    abuseSignalWindowSec: Int = 86400,
    abuseRegisterViolationMax: Int = 5,
    abuseRegisterPendingMax: Int = 3,
    abuseAuthLockoutMax: Int = 3,
    abuseBlockBaseSec: Int = 3600,
    abuseBlockMaxSec: Int = 604800,
    abuseStrikeDecaySec: Int = 2592000,
    securityAlertCooldownSec: Int = 900,
    securityAlertAnomalyMinCount: Int = 3,
) = AppConfig(
    port = 8080,
    databaseUrl = "postgresql://test:test@localhost:5432/testdb",
    authSecret = authSecret,
    isProduction = isProduction,
    corsAllowedOrigins = emptyList(),
    pbkdf2Iterations = pbkdf2Iterations,
    sessionMaxAgeSec = sessionMaxAgeSec,
    sessionAbsoluteMaxAgeSec = sessionAbsoluteMaxAgeSec,
    sessionRenewThresholdSec = sessionRenewThresholdSec,
    credentialsPrivateKeyPem = null,
    dataEncryptionKeyId = dataEncryptionKeyId,
    dataEncryptionKey = dataEncryptionKey,
    dataEncryptionKeys = dataEncryptionKeys,
    dataEncryptionAad = dataEncryptionAad,
    requireEncryptionAtRest = requireEncryptionAtRest,
    ollamaUrl = "http://localhost:11434",
    ollamaModel = "test",
    ollamaTimeoutMs = 5000,
    apiRateLimitWindowSec = 60,
    apiRateLimitMax = 180,
    infraRateLimitWindowSec = 60,
    infraRateLimitMax = 30,
    summaryRateLimitWindowSec = 60,
    summaryRateLimitMax = 10,
    changePasswordRateLimitWindowSec = 300,
    changePasswordRateLimitMax = 8,
    wsRateLimitWindowSec = 60,
    wsRateLimitMax = 30,
    limitCsrfWindowSec = 60,
    limitCsrfMax = 40,
    limitCredentialsWindowSec = 300,
    limitCredentialsMax = 12,
    limitSessionGetWindowSec = 60,
    limitSessionGetMax = 20,
    limitCredentialsKeyWindowSec = 60,
    limitCredentialsKeyMax = 20,
    limitRegisterWindowSec = 3600,
    limitRegisterMax = 6,
    limitRegisterBurstWindowSec = 600,
    limitRegisterBurstMax = 3,
    limitCredentialsAccountWindowSec = 900,
    limitCredentialsAccountMax = 50,
    lockoutFailThreshold = 5,
    lockoutBaseSec = 30,
    lockoutMaxSec = 1800,
    lockoutResetSec = 86400,
    alertIpFailureThreshold = 12,
    alertLockoutBurstSec = 900,
    signalAnomalyWindowSec = 86400,
    abuseSignalWindowSec = abuseSignalWindowSec,
    abuseRegisterViolationMax = abuseRegisterViolationMax,
    abuseRegisterPendingMax = abuseRegisterPendingMax,
    abuseAuthLockoutMax = abuseAuthLockoutMax,
    abuseBlockBaseSec = abuseBlockBaseSec,
    abuseBlockMaxSec = abuseBlockMaxSec,
    abuseStrikeDecaySec = abuseStrikeDecaySec,
    securityAlertCooldownSec = securityAlertCooldownSec,
    securityAlertAnomalyMinCount = securityAlertAnomalyMinCount,
    passwordProofChallengeTtlSec = passwordProofChallengeTtlSec,
    passwordProofMaxActive = passwordProofMaxActive,
    probeAppVersion = probeAppVersion,
    probeUpdateRequired = probeUpdateRequired,
    probeCompatibilityMode = probeCompatibilityMode,
    probeEncryptionKey = probeEncryptionKey,
    appleTeamId = "TEAM123456",
    iosBundleId = "com.ohmz.tday.ios",
    androidPackageName = androidPackageName,
    androidSha256CertFingerprints = androidSha256CertFingerprints,
    vapidPublicKey = null,
    vapidPrivateKey = null,
    retentionEventLogDays = 90,
    retentionAuthThrottleDays = 30,
    retentionAuthSignalDays = 180,
    retentionCronLogDays = 90,
    retentionDryRun = true,
    cspMode = null,
    cspConnectExtra = emptyList(),
    sentryDsn = null,
    sentryTracesSampleRate = 1.0,
    backendVersion = "0.0.0",
)
