-- Flyway V1 baseline migration
-- This captures the existing Prisma-managed schema as the starting point.
-- With baselineOnMigrate=true, Flyway will skip this on existing databases
-- and mark it as already applied.
--
-- Future schema changes should be added as V2__description.sql, V3__description.sql, etc.
-- The Exposed Table objects in db/tables/ must be updated to match.

SELECT 1;
