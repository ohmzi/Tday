export enum SortBy {
  dtstart = "dtstart",
  due = "due",
  duration = "duration",
  priority = "priority",
}

export enum GroupBy {
  dtstart = "dtstart",
  due = "due",
  duration = "duration",
  priority = "priority",
  rrule = "rrule",
  list = "list",
}

export enum Direction {
  Ascending = "Ascending",
  Descending = "Descending",
}

export enum UserRole {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum ApprovalStatus {
  PENDING = "PENDING",
  APPROVED = "APPROVED",
}
